<h3><%= 'Rediger afsnit' %></h3>

<h1>Opret nyt afsnit</h1>

<% form_for(@section, :html => {:id => 'faq_section_form'}, :method => :put) do |f| %>

<div>
  <div>
		<div class="form-row">
      <%= f.label(:title, "Afsnitstitel") %>
  		<%= f.text_field(:title, :class => "btn_gray") %>
    </div>
	</div>
	
	<div class="form-row">
		<%= f.submit t('save'), :name => "submit", :class => "btn_gray save" %>  | <%= link_button t('cancel'), faq_section_path(@section), 'cancel' %>
	</div>
</div>
<% end %>
<%= link_button t('go_back'), faqs_url, 'go_back' :title => t('go_back') %>
