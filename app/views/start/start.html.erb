<h2>Velkommen til CBCL</h2>
<p>
Du skal besvare nedenstående spørgeskema. Du begynder ved at trykke på start.

<div style=' width: 600px; padding: 20px; font-size: 16pt; background-color: #<%= @survey.color %>;' >
	<span class="survey_title"><%= link_button t('survey.start'), survey_url(@survey), 'survey_start' %></span>
	<%= @survey.description %>
</div>

<br>
<%= link_button t('finish'), logout_path, 'logout', :title => 'Log ud af systemet' %>