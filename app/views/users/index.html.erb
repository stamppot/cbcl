<h2>Liste af Brugere</h2>

<div class="commands">
<%= link_to t('user.new'), new_user_path, :title => 'Opret ny bruger' %>
</div>

<%= render :partial => 'shared/user_list' %>

<br/>
<p>


<div class="commands">
<% if current_user.has_access? :user_new_in_center %>
	<%= link_to t('user.new'), new_user_path(current_user.center), :title => 'Opret ny bruger i centeret' %>
<% elsif current_user.has_access? :user_new %>
	<%= link_to t('user.new'), new_user_path %>
<% end %>
</div>