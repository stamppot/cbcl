<h2>Rediger Bruger <%=h @user.login %></h2>

<% form_tag @user, :method => :put do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Anvend Ændringer', { :class => 'normal' } %>
<% end %>


<%= link_to t('show'), @user, :title => 'Vis bruger' %> |
<%= link_to t('go_back'), users_path, :title => t('go_back_to_list') %>
