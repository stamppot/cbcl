<h2>Rediger Bruger <%=h @user.login %></h2>

<% form_tag @user, :method => :put do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Anvend Ændringer', { :class => 'normal' } %>
<% end %>


<%= link_button t('show'), @user, 'user_show', :title => 'Vis bruger' %> |
<%= link_button t('go_back'), users_path, 'go_back' :title => t('go_back_to_list') %>
