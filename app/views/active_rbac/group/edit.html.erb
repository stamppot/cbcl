<h2>Redigering af gruppe</h2>

<% form_tag :action => :update, :id => @group do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Anvend Ændringer', { :class => 'normal' } %>
<% end %>

<%= link_to t('show'), :action => :show, :id => @group, :title => t('show') %> |
<%= link_to t('go_back'), {:action => :list}, :title => t('go_back_to_list') %>
