<h1>Rediger nationalitet</h1>

<% form_tag nationality_path(@nationality), :method => :put do %>
  <%= render :partial => 'form' %>
  <%= submit_tag t('edit'), :class => 'btn_gray' %>
<% end %>

<%= link_to t('show'), nationality_path(@nationality) %> |
<%= link_to t('go_back'), nationalities_path %>
